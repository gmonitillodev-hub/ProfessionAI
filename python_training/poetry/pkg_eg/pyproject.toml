[project]
name = "pkg-eg"
version = "0.1.0"
description = ""
authors = [
    {name = "Giuseppe Monitillo",email = "113453755+GMon11@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "tqdm (>=4.70.0,<5.0.0)"
]

[tool.poetry]
packages = [{include = "pkg_eg", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
